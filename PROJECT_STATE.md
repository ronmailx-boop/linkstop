# PROJECT_STATE - LinkStop

## Current Focus
שלב תכנון ארכיטקטורה ראשוני (טרם נכתב קוד אפליקציה). הוצגה למשתמש תוכנית מבנה
תיקיות, סכימת `share_target`, ומיפוי היכן Supabase נדרש בהכרח מול היכן אפשר
בלעדיו. ממתין לתשובות המשתמש לשאלות ההבהרה לפני תחילת בנייה בפועל.

## Decisions Made
- [x] נקרא CLAUDE.md; עקרון האבטחה הותאם ל-Supabase (RLS policies במקום Firestore Rules) לפי הנחיית המשתמש
- [x] סטאק מאושר: Frontend סטטי ב-GitHub Pages, Backend/DB = Supabase רק לפי הצורך בפועל
- [x] הוצעה תוכנית מבנה תיקיות ראשונית (כולל `docs/legal/`, `PROJECT_STATE.md` בשורש)
- [x] הוצע `share_target` עם `method: GET` (ולא POST) — אין צורך ב-Service Worker לתפיסת הבקשה כי אין שיתוף קבצים, רק טקסט/קישור
- [x] זוהה שSupabase Edge Function **נדרש בהכרח** לחילוץ מטא-דאטה (og:title/og:image וכו') בגלל מגבלת CORS על fetch מהקליינט לאתרים חיצוניים
- [ ] טרם הוחלט: persistence ב-localStorage בלבד (מכשיר יחיד) מול Supabase Postgres (ריבוי מכשירים)
- [ ] טרם הוחלט: האם נדרש Supabase Auth (אם כן משתמשים ב-DB, כדי ש-RLS יגן על הנתונים לפי משתמש)

## Open Questions (ממתין לתשובת המשתמש)
1. נדרש ריבוי מכשירים/סנכרון, או שמכשיר אנדרואיד יחיד עם localStorage מספיק ל-v1?
2. אם כן DB — נדרש Auth (login) כדי ש-RLS יבדיל בין משתמשים, או טבלה פתוחה אחת מספיקה?
3. הריפו ירוץ כ-Project Page (`username.github.io/linkstop/`) או דומיין מותאם אישית? (משפיע על `start_url`/`scope` ב-manifest)
4. יש לוגו/אייקונים קיימים ל-PWA, או צריך להכין (192/512px, maskable)?
5. נדרשת תמיכה offline לרשימת קישורים שכבר נשמרו, או מספיק חיבור לאינטרנט תמיד?
6. יש לתכנן מראש בסכימת ה-DB תכונות עתידיות (תיוג, חיפוש, ארכוב), או שעדיף מינימלי ל-v1?

## Next Step
לקבל תשובות המשתמש לשאלות לעיל, ואז לגבש Plan מפורט וסופי (מבנה קבצים + סכימת
DB אם רלוונטי + manifest סופי) ולקבל אישור מפורש **לפני** תחילת כתיבת קוד
האפליקציה בפועל (לפי כלל ה-Plan ב-CLAUDE.md).

## Files Changed
- `PROJECT_STATE.md` — נוצר (קובץ זה)
- אין קבצי קוד נוספים שנוצרו/שונו בשלב זה
